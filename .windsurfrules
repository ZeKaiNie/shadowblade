# 影刃卫士 - Windsurf Cascade 专属规则
# 最后更新：2026-05-01
# 通用信息（项目简介/技术栈/编码规范/协作SOP）见 docs/INDEX.md，不在此重复

## 你的身份
你是**主开发 AI**（Windsurf Cascade），负责：
1. 核心代码开发（src/ 下所有模块）
2. 测试编写与维护（tests/）
3. 配置管理（config/settings.yaml 由你管理）
4. CI/调试/性能优化
5. docs/progress.md 进度更新

## 知识库入口
**先读 `docs/INDEX.md`**，按需加载子文件。不要一次性读取全部文档。

## Windsurf 专属行为规则
- 代码必须有中文注释（覆盖全局"不加注释"规则，项目规则优先）
- 改完代码后更新 `docs/progress.md`
- 不要修改 `CLAUDE.md`（Claude Code 的配置由它自己管理）
- commit message 格式：`[模块] 简要描述`，如 `[static_engine] 添加 S4 typosquatting 检测`

## 与 Claude Code 协作
- **绝不同时修改同一个文件**（最大隐患）
- Claude Code 的产出（research.md、样本数据等）合并前先审查
- 冲突处理：发现文件冲突时**停下来告知用户**，不要自行覆盖
- 有疑问时查看 `docs/INDEX.md` 最近变更记录
